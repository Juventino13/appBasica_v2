  <div class="text-center">
<p id="notice"><%= notice %></p>

<h1>Detalles de ordenes</h1>

<%= link_to 'Agregar nuevo', new_detalle_orden_path %>

<hr> 
<div class="container">

<table class="table table-dark">
  <thead>
    <tr>
      <th>Producto </th>
      <th>Cantidad </th>
      <th>Precio </th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @detalle_ordens.each do |detalle_orden| %>
      <tr>
        <td><%= detalle_orden.producto %></td>
        <td><%= detalle_orden.cantidad %></td>
        <td><%= detalle_orden.precio %></td>
        <td><%= link_to 'Ver', detalle_orden %></td>
        <td><%= link_to 'Editar', edit_detalle_orden_path(detalle_orden) %></td>
        <td><%= link_to 'Eliminar', detalle_orden, method: :delete, data: { confirm: '¿Estas seguro de eliminar el detalle de la orden?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
</div>
<br>
