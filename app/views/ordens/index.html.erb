  <div class="text-center">
<p id="notice"><%= notice %></p>

<h1>Lista de ordenes </h1>

<%= link_to 'Nueva orden', new_orden_path %>
<hr> 
<div class="container">

<table class="table table-dark">
  <thead>
    <tr>
      <th>Cliente</th>
      <th>Num. orden</th>
      <th>Fecha</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @ordens.each do |orden| %>
      <tr>
        <td><%= orden.cliente %></td>
        <td><%= orden.num_orden %></td>
        <td><%= orden.fecha %></td>
        <td><%= link_to 'Ver', orden %></td>
        <td><%= link_to 'Editar', edit_orden_path(orden) %></td>
        <td><%= link_to 'Eliminar', orden, method: :delete, data: { confirm: '¿Estas seguro de eliminar la orden?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
</div>
<br>

